apiVersion: v1
kind: Service
metadata:
  name: devops-platform-svc
  namespace: dev-platform
spec:
  type: NodePort
  selector:
    app: devops-platform
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080