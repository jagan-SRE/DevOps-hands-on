# Day 19: Namespaces and RBAC

This exercise introduces a namespace-scoped Role and RoleBinding for reading
pods. The integrated chart under `charts/app` contains the complete
ServiceAccount, Role, and RoleBinding implementation used by the application.
