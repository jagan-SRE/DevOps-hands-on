# Message returned by the application's root endpoint.
APP_MESSAGE=Cloud Native DevOps Platform Running!

# Version label returned by the application's root endpoint.
APP_VERSION=development

# Docker registry repository used by the Jenkins pipeline.
IMAGE_REPOSITORY=jagan47/devops-45day-practice

# Kubernetes namespace targeted by local deployment commands.
KUBE_NAMESPACE=devops-practice
